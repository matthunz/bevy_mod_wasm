[package]
name = "bevy_mod_wasm_example_guest"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
bevy_mod_wasm = { path = "../..", default-features = false, features = ["guest"] }
bevy_mod_wasm_example_core = { path = "../core" }
