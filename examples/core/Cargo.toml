[package]
name = "bevy_mod_wasm_example_core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[features]
host = ["bevy"]

[dependencies]
serde = { version = "1", features = ["derive"] }
bevy = { version = "0.19.0", default-features = false, optional = true }
